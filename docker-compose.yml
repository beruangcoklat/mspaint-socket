version: "3"
services:
  ui:
    build: ./ui/.
    ports:
      - "80:80"
  api:
    build: ./api/.
    volumes:
      - "./api/node_modules:/app/node_modules"
  api2:
    build: ./api/.
    volumes:
      - "./api/node_modules:/app/node_modules"
